/** @type {import('next').NextConfig} */
const nextConfig = {
  // 로컬 개발: Next.js API Routes 사용 (mock 모드)
  // 프로덕션: NEXT_PUBLIC_API_URL=https://api.staysync.kr 설정 시 실제 백엔드 사용
}

export default nextConfig
